import React from 'react';
import {
  StyledView,
  StyledText,
  StyledScrollView,
  StyledImage,
  StyledButton,
} from '../../../common/StyledComponents';

const TopSales = () => {
  return (
    <StyledView></StyledView>
  )
}

export default TopSales;
